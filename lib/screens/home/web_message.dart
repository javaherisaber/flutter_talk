import 'package:flutter/material.dart';

class PlatformMessage extends StatelessWidget {
  const PlatformMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Hello from web only message');
  }
}
